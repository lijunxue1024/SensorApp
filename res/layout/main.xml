<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
>
    <TextView
            android:id="@+id/sensor_light_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    <TextView
            android:id="@+id/sensor_accelerate_text"
            android:layout_below="@+id/sensor_light_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    <TextView
            android:id="@+id/sensor_magnetic_text"
            android:layout_below="@+id/sensor_accelerate_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    <ImageView
            android:id="@+id/compass_img"
            android:layout_centerInParent="true"
            android:layout_width="600dp"
            android:layout_height="600dp"
            android:src="@drawable/ic_bg"
    />
    <ImageView
            android:id="@+id/arrow_img"
            android:layout_centerInParent="true"
            android:layout_width="220dp"
            android:layout_height="220dp"
            android:src="@drawable/ic_arrow"
    />

</RelativeLayout>

